约定
///////////////////////////////////
小驼峰命名

类 用大写开头

变量 函数名 小写开头

常量 全文字大写
/////////////////////////////////
Meditor 编写逻辑

config 加载配置

vo 是模型

gamedata里面定义 常量，全局， 

//////////////////////////////////////  git 步骤 约定
就 在 写好自己东西 的时候，需要push上去，再此之前必须
1、git pull origin master:**_branch ，先拉取master的东西和自己的分支合并，
2、解决冲突， 手动修改文档，  这个只有看着办、  看看那些文档有冲突，都要解决。
3、git add .
4 、git commit -m "注释一定要写，以后出问题了可以根据注释来退，告诉哪里改了什么加了什么越详细越好"
5、git push origin **_branch:master 把自己的代码push到远程的master上，这样我就可以pull最新的代码了。
--------------------------- 0.0冲突一定要解决。
